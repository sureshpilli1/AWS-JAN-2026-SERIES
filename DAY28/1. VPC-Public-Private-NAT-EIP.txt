AWS VPC, Public & Private Subnets, NAT Gateway, IGW, and Elastic IP Setup Guide
1. Introduction
In AWS, a **Virtual Private Cloud (VPC)** allows users to create an isolated network environment where they can deploy AWS resources securely. To manage internet access efficiently, AWS provides different components such as **public and private subnets, Internet Gateway (IGW), NAT Gateway, and Elastic IPs**.
This guide covers **theoretical concepts** and provides **a step-by-step setup process**.

2. Key Concepts
2.1 Virtual Private Cloud (VPC)
- A **VPC** is a logically isolated network in AWS.
- It allows users to define **IP address ranges, subnets, route tables, and security rules**.
2.2 Subnets (Public & Private)
- **Public Subnet:** Has a **route to the Internet Gateway (IGW)**, allowing instances to communicate with the internet.
- **Private Subnet:** Does **not** have a direct route to the internet. Instead, it uses a **NAT Gateway** for outgoing internet access.
2.3 Internet Gateway (IGW)
- An **IGW** enables **inbound and outbound internet access** for instances in a **public subnet**.
- It is attached at the **VPC level**.
2.4 NAT Gateway
- A **NAT Gateway** allows instances in a **private subnet** to access the internet while blocking inbound traffic.
- Requires an **Elastic IP (EIP)**.
2.5 Elastic IP (EIP)
- A **static public IP address** that you can attach to AWS resources.
- Used in **NAT Gateways** to maintain a constant public IP for outgoing requests.

3. Architecture Overview
The following AWS setup will be created:
1. **VPC** (`10.0.0.0/16`)
2. **Subnets:**
   - **Public Subnet** (`10.0.1.0/24`) - Connected to IGW
   - **Private Subnet** (`10.0.2.0/24`) - Connected to NAT Gateway
3. **Internet Gateway (IGW)**
4. **NAT Gateway** with **Elastic IP (EIP)**
5. **Route Tables** to define traffic flow

4. Step-by-Step Setup Guide
Step 1: Create a VPC
1. Go to **VPC Dashboard** → Click **Create VPC**.
2. Enter:
   - **Name:** `MyVPC`
   - **CIDR Block:** `10.0.0.0/16`
3. Click **Create VPC**.
Step 2: Create Public and Private Subnets
Public Subnet
1. Go to **Subnets** → Click **Create Subnet**.
2. Enter:
   - **Name:** `Public-Subnet`
   - **CIDR Block:** `10.0.1.0/24`  
3. Click **Create Subnet**.
4. enable **Auto-assign IP settings**.
Private Subnet
1. Repeat the above steps.
2. Enter:
   - **Name:** `Private-Subnet`
   - **CIDR Block:** `10.0.2.0/24`
Step 3: Create and Attach Internet Gateway (IGW)
1. Go to **Internet Gateways** → Click **Create Internet Gateway**.
2. Enter:
   - **Name:** `MyIGW`
3. Click **Attach to VPC** → Select `MyVPC`.
Step 4: Update Route Table for Public Subnet
1. Go to **Route Tables** → Click **Create Route Table**.
2. Enter:
   - **Name:** `Public-RT`
3. Click **Create**.
4. Edit **Routes**:
   - **Destination:** `0.0.0.0/0`
   - **Target:** `Internet Gateway (IGW)` → Select `MyIGW`
5. Associate this route table with **Public-Subnet**.
Step 5: Create a NAT Gateway for Private Subnet
1. Go to **NAT Gateways** → Click **Create NAT Gateway**.
2. Select:
   - **Subnet:** `Public-Subnet`
   - **Elastic IP:** Click **Allocate Elastic IP**.
3. Click **Create NAT Gateway**.
Step 6: Update Route Table for Private Subnet
1. Go to **Route Tables** → Click **Create Route Table**.
2. Enter:
   - **Name:** `Private-RT`
3. Click **Create**.
4. Edit **Routes**:
   - **Destination:** `0.0.0.0/0`
   - **Target:** `NAT Gateway` → Select `MyNATGateway`
5. Associate this route table with **Private-Subnet**.

5. Testing the Setup
Test Internet Access from Public Subnet
1. Launch an **EC2 instance** in **Public-Subnet**.
2. Assign a **Public IP** to it.
3. Connect via SSH and test with:
   ```sh
   ping google.com
Test Internet Access from Private Subnet
1. Launch an **EC2 instance** in **Private-Subnet** (No public IP).
2. Connect via SSH using a **Bastion Host** from the **Public Subnet**.
3. Run:
   ```sh
   ping google.com
   ```
   If successful, **NAT Gateway is working**.

2. Summary

| Component                  | Purpose                                                               |
| -------------------------- | --------------------------------------------------------------------- |
| **VPC**                    | Isolated network in AWS                                               |
| **Public Subnet**          | Internet access via Internet Gateway (IGW)                            |
| **Private Subnet**         | Uses NAT Gateway for internet access                                  |
| **IGW (Internet Gateway)** | Enables inbound and outbound internet traffic for public subnets      |
| **NAT Gateway**            | Allows outbound internet access for resources in private subnets      |
| **Elastic IP**             | Provides a static public IP address, commonly used with a NAT Gateway |

3. Additional Resources
- [AWS VPC Documentation](https://docs.aws.amazon.com/vpc/latest/userguide/what-is-amazon-vpc.html)
- [AWS Subnet Guide](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Subnets.html)
- [AWS NAT Gateway](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-nat-gateway.html)
```





Running notes:
-------------

up to now we are doing our practice in default VPC.
VPC is a region specific. as mumbai region has 3 availability zones 3 subnets are present by default.
as a default internet gate way is present we are accessing internet.
CIDR of a default VPC is 172.31.0.0/16.
VPC is region specific service.
we can generate 2 power 16 hosts with the above CIDR
when we are creating our own VPC we can take our own private IP addresses
in any project min 6 subnets are present.
we have to give CIDR for every VPC. same CIDR should not be given for two VPC. CIDR overlaping should not be there.
based on how many instances we are launching we have to calculate.
suppose we want to deploy a web application frontend, backend and database, we create a subnet for every thing. 
based on CIDR we can launch the number of instances in a subnet.
5 IPs are reserved in every subnet in AWS
.0 (network address) the base of the subnet range.
.1 (VPC router) reserved by AWS for routing traffic
.2 (DNS server) the base plus two used for AWS DNS service
.3 (future use ) reserved by the AWS for future fuctionality
.255 (broad cost address) the last address in the subnet 


Suppose our requirement is to launch 200 machines in every subnet how do we define subnet. go to DAVIDCNET(vusual subnet calculator). give the network address and mask bits and update it. 
Click on devide untill you get the required number of hosts. give that subnet ID to first subnet and for next subnet we use next CIDR.
Remaining range we keep for future requirement for examples i want to launch 40 machine, take the CIDR we kept aside and paste it network address and mask bits of DAVIDCNET and again devide them until you get 
the required number of hosts. you can merge the CIDR if required.
in AWS /28 is the minimum.
by default all subnets in a VPC are inter connected. there will be a local route


in general we launch web server in public subnet. application server and DB server are launched in private subnet.
in subnet list of AWS number of available IP is shown it's number decreases as we launch instances
instances are launched in same region where VPC is present.
in network settings while launching an instace select the VPC we created and enable auto asign public IP. 
In class we created two instances one in private subnet and other in public subnet and tried to connect but failed because there is no internet gateway
every VPC has only one IGW
create one IGW and attach to VPC.
after attaching also instance don't know how to connect to IGW so we need to create a rout table
goto VPC click on create route tabel. after creating RT edit the routing rules and add IGW in the target of RT
after that subnet is associated with RT. go to RT subnet assiciations edit and select the public subnet only. donot select the private subnet.
ssh the instance with public IP
public and private subnets has link as they are in same VPC
create RT name it as private route table and associate it with private subnet.
donot add IGW record to private route table 
Observe the routes in public and private route table there will be a local route in both, by these local routees instances present in private and public subnets are communicating. 
So we can connect to public instace and that instace will connect to private instance.
copy the .pem file of the private subnet in the public instace and connect from public instance to private instance.
go to private instance (application server) and connect using private IP take the ssh and paste in cli.
ping google.com from private instance. there is no internet connection we should not add IGW private route table. we have to create a NAT GATEWAY in the public subnet to give internet connectivity ot private instace.
search NAT in aws and create. NAT is chargeable 
NAT record is added in private RT, then private ec2 get internet access. This done in some patching work.
