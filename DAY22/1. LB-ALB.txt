AWS Load Balancer & EC2 Scaling – Full  Notes
This document explains the concepts of Load Balancing and EC2 Scaling, followed by a practical setup of an **Application Load Balancer (ALB)** in a VPC environment with two or more EC2 instances.
What is a Load Balancer?
A **Load Balancer** in AWS is a service that automatically distributes incoming application traffic across multiple targets (like EC2 instances, containers, or IPs) in one or more Availability Zones. 
It helps achieve **high availability**, **fault tolerance**, and **horizontal scalability**.

Types of AWS Load Balancers
1. **Application Load Balancer (ALB)**
   * Works at **Layer 7** (HTTP/HTTPS)
   * Supports **host-based and path-based routing**
   * Good for web apps, REST APIs, microservices
2. **Network Load Balancer (NLB)**
   * Works at **Layer 4** (TCP)
   * Very high performance, low latency
   * Suitable for real-time apps, gaming, VoIP
3. **Classic Load Balancer (CLB)** (Legacy)
   * Works at both Layer 4 and Layer 7
   * Not recommended for new deployments
4. **Gateway Load Balancer (GWLB)**
   * Designed for **third-party virtual appliances**
   * Handles security appliances like firewalls, intrusion detection

Scaling EC2 Instances

Vertical Scaling
* Increase the **instance size** (e.g., `t2.micro` → `t3.large`)
* Requires **stopping the instance** before changing type
* Used when single instance needs more power (RAM/CPU)
* Not fault-tolerant (still a single point of failure)
Steps:
1. Stop EC2 instance
2. Modify instance type under **Actions → Instance Settings → Change Instance Type**
3. Start the instance again

Horizontal Scaling
* Add **more EC2 instances** and place them behind a Load Balancer
* Distributes traffic across instances
* Increases availability and fault tolerance
* Works well with **Auto Scaling Groups**

Scenario: Load Balancer with Two Web Servers (httpd)
Step 1: Launch Two EC2 Instances (Web Servers)
1. Use Amazon Linux 2
2. Install Apache (HTTPD)
```bash
sudo yum update -y
sudo yum install httpd -y
sudo systemctl enable httpd
sudo systemctl start httpd
```
3. Customize index.html:
```bash
echo "<h1>Web Server 1</h1>" | sudo tee /var/www/html/index.html (tee reads input and writes it to a file.)
```
Repeat on second instance:
```bash
echo "<h1>Web Server 2</h1>" | sudo tee /var/www/html/index.html
```
Step 2: Security Group for EC2
* Allow **port 80 (HTTP)** from anywhere (0.0.0.0/0)

Step 3: Create an Application Load Balancer (ALB)
1. Go to EC2 → Load Balancers → Create Load Balancer
2. Choose "Application Load Balancer"
3. Basic Configuration:
* Name: `my-alb`
* Scheme: Choose **Internet-facing** (for public access)
* IP type: IPv4
4. Network Mapping:
* Select appropriate VPC
* **Choose at least 2 subnets**, each from a different Availability Zone
5. Security Groups:
* Create or select a Security Group with **port 80 open** (inbound rule)
6. Listeners and Routing:
* Listener protocol: HTTP (port 80)
* Default action: Forward to **Target Group** (create a new one below)

Step 4: Create a Target Group (TG)
1. Choose Target Type: **Instance**
2. Protocol: HTTP, Port: 80
3. Health Checks:
* Path: `/`
* Protocol: HTTP
* Healthy threshold: 3
* Unhealthy threshold: 2
4. Register Targets:
* Select the two EC2 instances you launched
* Click **Include as pending below**
5. Create Target Group

Step 5: Complete Load Balancer Creation
* Attach the newly created Target Group to the Load Balancer
* Review and click **Create Load Balancer**
After creation:
* Copy the **DNS name** of the ALB
* Open in browser:

```
http://<ALB-DNS-name>
```
* You will see Web Server 1 or Web Server 2 randomly (based on routing)

Step 6: Sticky Sessions (Session Affinity)
* Sticky Sessions ensure that the same client is routed to the same target
* You can enable it in the **Target Group settings**
To Enable Stickiness:
1. Go to EC2 Console → Target Groups
2. Select your Target Group → Edit Attributes
3. Enable **Stickiness**
4. Set duration (e.g., 300 seconds)
Disable the same way if needed.

Step 7: Add Another Target Group and Listener (Port 8080)
1. Launch a third EC2 instance → Install HTTPD
2. Update index.html:
```bash
echo "<h1>Third Server (8080)</h1>" | sudo tee /var/www/html/index.html
```
3. Create new Target Group:
* Name: `tg-8080`
* Protocol: HTTP, Port: 8080
* Register the third EC2 instance
4. Add new Listener:
* Go to Load Balancer → Listeners → Add Listener
* Port: 8080
* Forward to `tg-8080`
5. Open in browser:

```
http://<ALB-DNS-name>:8080
```
You’ll see “Third Server (8080)” page

Additional  Points
* Load balancers **auto-distribute** traffic based on health
* Health checks ensure only healthy targets receive requests
* Sticky sessions are useful for session-based apps
* Port-based routing enables hosting multiple apps on the same ALB
* Use **Auto Scaling** to dynamically register/unregister instances

This completes a real-world ALB configuration with multiple EC2s, horizontal scaling, stickiness, and custom listeners. 
This approach mimics what companies use for high availability and traffic management.




explaination by chatgpt:
-----------------------

What is Fault Tolerance?
Fault tolerance means:
A system continues to work even if one or more components fail.
Example
Suppose your application is running on only one EC2 instance:
If EC2-1 crashes:
Users
  |
  X
EC2-1 (Failed)
The application becomes unavailable.
This system is not fault tolerant.

Fault-Tolerant Setup
Run multiple EC2 instances:
          +--------+
Users --->|  Load  |
          |Balancer|
          +--------+
            /    \
           /      \
      EC2-1      EC2-2
If EC2-1 fails:
          +--------+
Users --->|  Load  |
          |Balancer|
          +--------+
                \
                 \
                EC2-2
Users can still access the application through EC2-2.
This is fault tolerance.





what is OSI model:
------------------
What is the OSI Model?
OSI stands for:
Open Systems Interconnection
It is a model that explains how data travels from one computer to another over a network.
Think of it as a 7-step journey for data.
For example, when you open a website:
Your Browser
     |
     v
Internet
     |
     v
Web Server

The data passes through 7 layers on the sender side and 7 layers in reverse on the receiver side.
The 7 Layers
7. Applition
6. Presentation
5. Session
4. Transport
3. Network
2. Data Link
1. Physical

A common memory trick:

All
People
Seem
To
Need
Data
Processing

Layer 7 – Application Layer
This is the layer closest to the user.The Application Layer (Layer 7) is the layer that directly interacts with applications used by humans.
Simple Definition
The Application Layer provides network services to end-user applications and defines how applications communicate over a network.

Protocols:
HTTP
HTTPS
FTP
SMTP
DNS

Example:
When you type:
https://www.google.com
your browser uses HTTP/HTTPS at the Application Layer.

Real-Life Analogy
You write a letter.
Dear Friend,
How are you?
This is the actual message.

Layer 6 – Presentation Layer
Responsible for:
Encryption
Decryption
Compression
Data formatting

Example:
HTTPS uses encryption.
Hello
might become:
X7#kL9@
before being sent.
The receiving side decrypts it.
Analogy
You decide to write the letter in English, Telugu, or Hindi.

Layer 5 – Session Layer
Responsible for:
Creating connections
Maintaining connections
Closing connections
Example:
You log in to a website.
The session layer helps maintain the conversation between your browser and the server.
Analogy
A phone call starts, continues, and ends.

Layer 4 – Transport Layer
Responsible for:
Breaking data into segments
Reliable delivery
Port numbers

Protocols:
TCP
UDP

TCP
Reliable:
Send packet
Receive acknowledgment
Used by:
HTTP
HTTPS
SSH

UDP
Fast but unreliable:
Send packet
No acknowledgment

Used by:
Video streaming
Gaming
DNS

Ports
Examples:
80   HTTP
443  HTTPS
22   SSH
3306 MySQL

Analogy
Suppose a book has 100 pages.
Transport Layer divides it into smaller pieces and ensures all pieces arrive.

Layer 3 – Network Layer
Responsible for:
IP addressing
Routing

Protocol:
IP
Example:
192.168.1.10
10.0.0.5
8.8.8.8
Routers work here.
Analogy
Writing the destination address on an envelope:
House No. 10
Street ABC
City XYZ
Without an address, the post office doesn't know where to send it.

Layer 2 – Data Link Layer
Responsible for:
Communication within a local network
MAC addresses

Example MAC address:
00:1A:2B:3C:4D:5E
Switches work here.
Analogy
Inside a city, the local post office decides which street gets the letter.

Layer 1 – Physical Layer
Responsible for:
Electrical signals
Fiber-optic signals
Network cables
Wi-Fi radio signals
Examples:
Ethernet cable
Fiber cable
Wireless signals
Analogy
The roads on which the postal truck travels.

Complete Example: Opening Google
You type:
https://www.google.com
Layer 7 (Application)
Browser creates an HTTP request.
Layer 6 (Presentation)
Request is encrypted using HTTPS.
Layer 5 (Session)
Connection is established.
Layer 4 (Transport)
Data is split into TCP segments.
Layer 3 (Network)
Destination IP address is attached.
Layer 2 (Data Link)
MAC addresses are added.
Layer 1 (Physical)
Bits travel through Wi-Fi or cable.

Why DevOps Engineers Mainly Care About Layers 3, 4, and 7
Since you're learning DevOps, focus on these:
| Layer | Important Concepts                   |
| ----- | ------------------------------------ |
| 7     | HTTP, HTTPS, REST APIs, ALB          |
| 4     | TCP, UDP, Ports, NLB                 |
| 3     | IP Addressing, Routing, Subnets, VPC |
| 2     | MAC Addresses, Switches              |
| 1     | Cables, Network Interfaces           |


Most AWS, Docker, Kubernetes, and Load Balancer questions revolve around Layers 3, 4, and 7.

Example in AWS
Application Load Balancer → Layer 7
Network Load Balancer → Layer 4
VPC Routing Tables → Layer 3
Security Groups and Network ACLs mainly work with Layer 3/4 information (IPs, protocols, ports)

If you'd like, I can also explain how a packet travels from your laptop to Google step by step through all 7 OSI layers, which makes the model much easier to remember.
yes
Great! Let's follow a real packet from your laptop to Google and see what happens at each OSI layer.

Suppose you open a browser and type:
https://www.google.com

Step 1: Application Layer (Layer 7)
Your browser creates an HTTP request:
GET / HTTP/1.1
Host: www.google.com
This is the actual message you want to send.
Think:
"I want Google's homepage."
HTTP stands for:
HyperText Transfer Protocol
HTTP is a protocol, meaning a set of communication rules.
It defines:
How a browser asks for a page.
How a server responds.
The format of requests and responses.

Step 2: Presentation Layer (Layer 6)
Since you're using HTTPS, the data is encrypted using TLS/SSL.
Before encryption:
GET / HTTP/1.1
Host: www.google.com
After encryption:
8h3K#@2LmP...
Anyone intercepting the traffic sees only encrypted data.

Step 3: Session Layer (Layer 5)
A communication session is established between:
Your Browser <-----> Google Server
The session layer helps maintain and manage this conversation.

Step 4: Transport Layer (Layer 4)
The Transport Layer uses TCP.
First, TCP establishes a connection using the 3-way handshake:
Laptop                        Google
SYN       ------------------->
          <------------------- SYN-ACK
ACK       ------------------->

Connection established.

Now TCP adds:

Source Port      : 50000
Destination Port : 443

Example:
192.168.1.10:50000 ---> Google:443
Port 443 means HTTPS.

Step 5: Network Layer (Layer 3)
Now an IP header is added.
Example:

Source IP      : 192.168.1.10
Destination IP : 142.250.183.36

(One of Google's IP addresses)

Packet becomes:

Source IP      : 192.168.1.10
Destination IP : 142.250.183.36
TCP Port       : 443
Data           : Encrypted HTTP Request

Routers use this information to decide where to send the packet.

Step 6: Data Link Layer (Layer 2)
Now MAC addresses are added.
Example:
Source MAC      : AA:BB:CC:DD:EE:FF
Destination MAC : 11:22:33:44:55:66
These MAC addresses are used only on the local network.
The packet is now inside an Ethernet frame.

Step 7: Physical Layer (Layer 1)
The frame is converted into actual signals:
Electrical signals (Ethernet cable)
Light signals (Fiber)
Radio waves (Wi-Fi)

These signals travel to your router.

Journey Through the Internet

Your home router receives the packet.

Laptop
   |
   v
Home Router
   |
   v
ISP
   |
   v
Internet Routers
   |
   v
Google Network
   |
   v
Google Server

At each router:
Layer 2 information changes
Layer 3 IP addresses remain the same
Routers use the destination IP to forward the packet.

At Google's Server
The server receives the packet and removes the headers in reverse order.
Layer 1 -> Layer 2 -> Layer 3 -> Layer 4 -> Layer 5 -> Layer 6 -> Layer 7
This process is called decapsulation.

Finally Google sees:
GET / HTTP/1.1
Host: www.google.com
Google processes the request and sends a response back.


Encapsulation and Decapsulation

When sending:

Application Data
      |
      v
+------------------+
| Layer 7 Data     |
+------------------+
| Layer 4 Header   |
+------------------+
| Layer 3 Header   |
+------------------+
| Layer 2 Header   |
+------------------+
| Physical Signals |
+------------------+

This is called encapsulation.

When receiving:

Remove Layer 2 Header
Remove Layer 3 Header
Remove Layer 4 Header
Decrypt Data
Deliver to Application

This is called decapsulation.

What DevOps Engineers Usually See

When you work with Docker, Kubernetes, Linux, and AWS:

Layer 7
HTTP
HTTPS
REST APIs
Ingress
Application Load Balancer
Layer 4
TCP
UDP
Ports
Network Load Balancer

Examples:

22    SSH
80    HTTP
443   HTTPS
3306  MySQL
Layer 3
IP addresses
Subnets
Routing
VPC
Route Tables

Examples:

10.0.1.5
192.168.1.10
172.31.0.0/16
Easy Memory Trick

Think of sending a courier:

OSI Layer	Courier Analogy
7 Application	Letter you wrote
6 Presentation	Encrypt/translate letter
5 Session	Start and maintain conversation
4 Transport	Break letter into packages
3 Network	Write destination address
2 Data Link	Local delivery instructions
1 Physical	Road, truck, cable, Wi-Fi

For DevOps interviews, if you thoroughly understand Layer 3 (IPs), Layer 4 (TCP/UDP/Ports), and Layer 7 (HTTP/HTTPS), you'll be able to understand most networking questions in AWS, Docker, Kubernetes, 
and load balancers.




protocols explaination:
What is a Protocol?
A protocol is simply a set of rules that two systems agree to follow when communicating.
Real-life example
Suppose you call customer support.
There is a protocol:
1. Say Hello
2. Tell your issue
3. Answer questions
4. Receive solution
5. End the call
Both sides follow the same rules.
Similarly, computers need rules to communicate.
Those rules are called protocols.


Protocol Summary Cheat Sheet

Layer 7 (Application Layer)
These are the protocols you interact with most often.
| Protocol | Port | Purpose                   |
| -------- | ---- | ------------------------- |
| HTTP     | 80   | Web browsing              |
| HTTPS    | 443  | Secure web browsing       |
| SSH      | 22   | Remote server login       |
| FTP      | 21   | File transfer             |
| SFTP     | 22   | Secure file transfer      |
| SMTP     | 25   | Sending email             |
| POP3     | 110  | Receiving email           |
| IMAP     | 143  | Accessing email on server |
| DNS      | 53   | Domain name resolution    |


Layer 4 (Transport Layer)
Only two protocols are extremely important.
| Protocol | Purpose                |
| -------- | ---------------------- |
| TCP      | Reliable communication |
| UDP      | Fast communication     |

Layer 3 (Network Layer)
IP
Full Form: Internet Protocol
ICMP
Full Form: Internet Control Message Protocol
Used by:
ping google.com
Purpose:
Network diagnostics
Reachability testing
Important Notes
No port numbers
Used for Ping and Traceroute



Most Important Ports to Remember
| Port  | Protocol       | Purpose             |
| ----- | -------------- | ------------------- |
| 22    | SSH            | Remote login        |
| 53    | DNS            | Name resolution     |
| 80    | HTTP           | Web traffic         |
| 443   | HTTPS          | Secure web traffic  |
| 21    | FTP            | File transfer       |
| 25    | SMTP           | Email sending       |
| 3306  | MySQL          | Database            |
| 5432  | PostgreSQL     | Database            |
| 6379  | Redis          | Cache               |
| 27017 | MongoDB        | Database            |
| 8080  | HTTP Alternate | Applications/Tomcat |




AWS and DevOps Mapping
| Technology                | Protocols Used       |
| ------------------------- | -------------------- |
| EC2 SSH Login             | SSH → TCP → IP       |
| Website on EC2            | HTTPS → TCP → IP     |
| Kubernetes API            | HTTPS → TCP → IP     |
| Docker Registry           | HTTPS → TCP → IP     |
| AWS Console               | HTTPS → TCP → IP     |
| Route 53                  | DNS                  |
| Application Load Balancer | HTTP/HTTPS (Layer 7) |
| Network Load Balancer     | TCP/UDP (Layer 4)    |



| Protocol | Remember This                         |
| -------- | ------------------------------------- |
| HTTP     | Web traffic, Port 80                  |
| HTTPS    | Secure web traffic, Port 443          |
| SSH      | Remote login, Port 22                 |
| DNS      | Converts domain names to IP addresses |
| TCP      | Reliable communication                |
| UDP      | Fast communication                    |
| IP       | Addressing and routing                |
| ICMP     | Ping and diagnostics                  |
| DHCP     | Automatically assigns IP addresses    |
| ARP      | Finds MAC address from IP address     |


| Application Protocol | Transport Protocol Used       |
| -------------------- | ----------------------------- |
| HTTP                 | TCP                           |
| HTTPS                | TCP                           |
| SSH                  | TCP                           |
| FTP                  | TCP                           |
| SMTP                 | TCP                           |
| POP3                 | TCP                           |
| IMAP                 | TCP                           |
| DNS                  | UDP (mostly), TCP (sometimes) |
| DHCP                 | UDP                           |
| SNMP                 | UDP                           |
| NTP                  | UDP                           |



| Protocol       | Full Form                             | Layer                     | Default Port | Used For                    | Important Notes                                        | Example                              |
| -------------- | ------------------------------------- | ------------------------- | ------------ | --------------------------- | ------------------------------------------------------ | ------------------------------------ |
| HTTP           | HyperText Transfer Protocol           | Application (L7)          | 80           | Web browsing, APIs          | Stateless, unencrypted, uses TCP                       | Opening `http://example.com`         |
| HTTPS          | HyperText Transfer Protocol Secure    | Application (L7)          | 443          | Secure web browsing, APIs   | HTTP + TLS/SSL encryption, uses TCP                    | Opening `https://google.com`         |
| SSH            | Secure Shell                          | Application (L7)          | 22           | Remote server login         | Secure, encrypted, uses TCP                            | `ssh ec2-user@server`                |
| FTP            | File Transfer Protocol                | Application (L7)          | 21           | File transfer               | Unencrypted, uses TCP                                  | Uploading files to a server          |
| SFTP           | SSH File Transfer Protocol            | Application (L7)          | 22           | Secure file transfer        | Runs over SSH, encrypted                               | Securely copying files               |
| SMTP           | Simple Mail Transfer Protocol         | Application (L7)          | 25           | Sending email               | Mail transfer protocol, uses TCP                       | Gmail sending an email               |
| POP3           | Post Office Protocol v3               | Application (L7)          | 110          | Receiving email             | Downloads emails to local device                       | Outlook downloading mail             |
| IMAP           | Internet Message Access Protocol      | Application (L7)          | 143          | Accessing email on server   | Keeps emails on server, syncs devices                  | Gmail accessed from phone and laptop |
| DNS            | Domain Name System                    | Application (L7)          | 53           | Domain-to-IP resolution     | Usually uses UDP, sometimes TCP                        | `google.com → 142.x.x.x`             |
| DHCP           | Dynamic Host Configuration Protocol   | Application (L7)          | 67/68        | Assigning IP addresses      | Automatically provides IP, gateway, DNS                | Laptop gets IP from Wi-Fi router     |
| SNMP           | Simple Network Management Protocol    | Application (L7)          | 161/162      | Monitoring network devices  | Used by monitoring tools                               | Monitoring switches and routers      |
| NTP            | Network Time Protocol                 | Application (L7)          | 123          | Time synchronization        | Uses UDP                                               | Syncing server time                  |
| LDAP           | Lightweight Directory Access Protocol | Application (L7)          | 389          | Directory services          | User authentication and lookup                         | Active Directory integration         |
| TCP            | Transmission Control Protocol         | Transport (L4)            | N/A          | Reliable communication      | Connection-oriented, acknowledgements, retransmissions | HTTP, HTTPS, SSH traffic             |
| UDP            | User Datagram Protocol                | Transport (L4)            | N/A          | Fast communication          | Connectionless, no acknowledgements                    | DNS queries, streaming, gaming       |
| IP             | Internet Protocol                     | Network (L3)              | N/A          | Addressing and routing      | Provides source and destination IPs                    | `192.168.1.10 → 8.8.8.8`             |
| ICMP           | Internet Control Message Protocol     | Network (L3)              | N/A          | Network diagnostics         | Used by ping and traceroute                            | `ping google.com`                    |
| ARP            | Address Resolution Protocol           | Network/Data Link (L2/L3) | N/A          | IP-to-MAC mapping           | Finds MAC address for an IP                            | Finding router MAC address           |
| Ethernet       | Ethernet Protocol                     | Data Link (L2)            | N/A          | Local network communication | Uses MAC addresses                                     | Office LAN communication             |
| Wi-Fi (802.11) | IEEE 802.11 Wireless Protocol         | Data Link (L2)            | N/A          | Wireless networking         | Wireless version of Ethernet-like communication        | Laptop connected to Wi-Fi            |





What is a Web Server?
A web server is software that:
Receives requests from clients (browsers) and sends back web content such as HTML pages, images, CSS files, JSON data, etc.
Examples of web servers:
Apache HTTP Server (httpd)
Nginx
Microsoft IIS
What Does a Web Server Do?
Suppose your browser requests:
http://54.100.10.20
The browser sends a request.
The web server:
Receives the request.
Finds the requested file.
Sends the file back.
Example:
Browser
   |
   | HTTP Request
   v
Apache (httpd)
   |
   | Reads
   v
index.html
   |
   v
Browser Displays Page


Relationship Between Web Server and HTTP
This is the key idea:
HTTP
Defines the rules.
Web Server
Follows those rules.

Example Flow
You type:
http://example.com
Step 1
Browser creates an HTTP request:
GET /
Host: example.com
Step 2
Request reaches Apache (httpd).
Browser
   |
   v
Apache
Step 3
Apache looks for:
/var/www/html/index.html
Step 4
Apache reads:
<h1>Welcome</h1>
Step 5
Apache sends an HTTP response:
HTTP/1.1 200 OK
<h1>Welcome</h1>
Step 6
Browser renders:
Welcome


1. What is httpd?
httpd stands for:
HTTP Daemon
It is the Apache web server process running on Linux.
When you install Apache
a web server is installed.

The httpd service:
Listens on port 80 (HTTP)
Receives browser requests
Finds the requested file
Sends the file back to the browser
Example:
Client Browser
      |
      | GET /
      v
    httpd

2. What is HTML?
HTML stands for:
HyperText Markup Language
It is the language used to create web pages.
Example:
<h1>Hello World</h1>
<p>Welcome to my website</p>
HTML defines:
Headings
Paragraphs
Images
Tables
Forms
Links
HTML is content, not a server.

3. What is index.html?
index.html is simply a file containing HTML.
Example:
File:
/var/www/html/index.html
Content:
<h1>Web Server 1</h1>
The filename is special because web servers look for it by default.


What is HTML?
HTML stands for:
HyperText Markup Language
It is the language used to create web pages.
Example:
<h1>Hello World</h1>
<p>Welcome to my website</p>
HTML defines:
Headings
Paragraphs
Images
Tables
Forms
Links
HTML is content, not a server.



Quick HTML Tag Cheat Sheet

| Tag              | Purpose        |
| ---------------- | -------------- |
| `<h1>` to `<h6>` | Headings       |
| `<p>`            | Paragraph      |
| `<a>`            | Link           |
| `<img>`          | Image          |
| `<table>`        | Table          |
| `<tr>`           | Table Row      |
| `<th>`           | Table Header   |
| `<td>`           | Table Data     |
| `<form>`         | Form           |
| `<input>`        | Input Field    |
| `<ul>`           | Unordered List |
| `<ol>`           | Ordered List   |
| `<li>`           | List Item      |
| `<br>`           | Line Break     |



What is a Daemon?
A daemon is a background process that runs continuously and provides services or performs tasks without direct user interaction. 
Examples include httpd (Apache), sshd (SSH server), and dockerd (Docker daemon).
In Linux/Unix, a daemon is a program that runs in the background and provides a service.
It usually:
Starts automatically when the system boots.
Runs continuously.
Waits for requests or events.
Has no direct user interface.

Examples of Linux Daemons
| Daemon    | Purpose                |
| --------- | ---------------------- |
| `httpd`   | Apache web server      |
| `sshd`    | SSH server             |
| `crond`   | Runs scheduled jobs    |
| `systemd` | System/service manager |
| `dockerd` | Docker daemon          |
